/**
 * Supabase configuration helpers.
 * @see lib/env.ts for full environment validation.
 */
export {
  isPublicSupabaseConfigured as isSupabaseConfigured,
  isAdminSupabaseConfigured as isSupabaseAdminConfigured,
} from "@/lib/env";
